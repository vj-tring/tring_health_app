

// Application environment options
enum APP_ENVIRONMENT {
  dev = 'dev', // Development
  production = 'production', // Production
  demo = 'demo', // Demo
}

// Default environment for the app
export const DEFAULT_ENVIRONMENT: APP_ENVIRONMENT = APP_ENVIRONMENT.demo;
